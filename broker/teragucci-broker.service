[Unit]
Description=Teragucci Connection Broker
After=network.target syslog.target
Wants=network-online.target

[Service]
Type=simple
User=root
Group=root
WorkingDirectory=/opt/teragucci
ExecStart=/usr/bin/python3 -m broker.main \
    --host 0.0.0.0 \
    --port 8443 \
    --config /etc/teragucci/broker.yml \
    --secret /etc/teragucci/broker.secret \
    --tls-cert /etc/teragucci/tls/broker.crt \
    --tls-key /etc/teragucci/tls/broker.key \
    --ipa-servers ldap://dxs-salt.the-dxs.com,ldap://dxs-salty.the-dxs.com \
    --ipa-base-dn dc=the-dxs,dc=com \
    --required-group teragucci-users \
    --admin-group teragucci-admins

Restart=on-failure
RestartSec=5
StandardOutput=journal
StandardError=journal
SyslogIdentifier=teragucci-broker

# Security hardening
NoNewPrivileges=yes
ProtectSystem=strict
ProtectHome=read-only
ReadWritePaths=/var/log/teragucci
PrivateTmp=yes

[Install]
WantedBy=multi-user.target
